# Core dependencies
flask>=3.0.0
mcp>=1.9.0

# Search backend (optional — app falls back to SQLite if ES is unreachable)
elasticsearch>=8.0.0

# Data processing
pandas>=2.0.0

# gRPC / Protobuf (contract is illustrative; compile .proto with grpc_tools)
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=5.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
